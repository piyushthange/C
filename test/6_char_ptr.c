#include <stdio.h>
#include <string.h>

int main(int argc, char *agrv[]){
	char str[20] = "hello start";
	char *ptr = "hello pointer";

	printf("str array = %s\n", str);
	printf("pointer char = %s\n", ptr);

	return 0;
}
